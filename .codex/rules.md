# Repository Rules

- After finishing each change set, create a small git commit.
- Keep preserved tutorial and reference material under `ref/`, outside `src/`.
- Treat `data/resume.tex`, `data/resume.pdf`, and `data/resume.txt` as the source material for resume-driven content updates.
